# --- Baza danych ---
# Dla SQLite (domyślnie):
DATABASE_URL="file:./dev.db"
# Lub PostgreSQL:
# DATABASE_URL="postgresql://user:pass@localhost:5432/telemed?schema=public"

# --- Admin panel ---
ADMIN_PASSWORD="admin"

# --- Stripe ---
STRIPE_SECRET_KEY="sk_live_or_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..." # z panelu Stripe > Developers > Webhooks
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_or_test_..."

# --- Domeny (dla URL-i sukcesu/porazki w checkout) ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- E-mail (opcjonalnie) ---
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASS="password"
FROM_EMAIL="no-reply@twojadomena.pl"
RECEPCJA_EMAIL="recepcja@twojadomena.pl"